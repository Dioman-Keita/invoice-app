<!doctype html>
<html lang="fr">
<head>
  <meta charset="utf-8">
  <title>Fournisseur {{name}}{{#if id}} ({{id}}){{/if}}</title>
  <style>
    body { font-family: Arial, Helvetica, sans-serif; color: #111; margin: 24px; }
    .header { display: flex; align-items: center; margin-bottom: 16px; }
    .brand { color: #1a472a; font-weight: bold; font-size: 18px; }
    .muted { color: #666; font-size: 12px; }
    h1 { color: #1a472a; font-size: 20px; margin: 12px 0; }
    .kv { display: grid; grid-template-columns: 220px 1fr; gap: 8px 16px; margin: 12px 0 24px; }
    .kv .k { color: #1a472a; font-weight: bold; }
    .footer { display: flex; justify-content: space-between; margin-top: 16px; color: #666; font-size: 11px; }
  </style>
</head>
<body>
  <div class="header">
    <div>
      <div class="brand">COMPAGNIE MALIENNE POUR LE DÉVELOPPEMENT DU TEXTILE (CMDT)</div>
      <div class="muted">Export Fournisseur</div>
    </div>
  </div>

  <h1>Détails du fournisseur</h1>
  <div class="kv">
    <div class="k">Référence</div><div>{{id}}</div>
    <div class="k">Nom fournisseur</div><div>{{name}}{{supplier_name}}</div>
    <div class="k">Numéro de compte</div><div>{{account_number}}{{supplier_account}}</div>
    <div class="k">Téléphone</div><div>{{phone}}</div>
    <div class="k">Créée le</div><div>{{create_at}}</div>
  </div>

  <div class="footer">
    <div>Généré le {{_generated_at}}</div>
    <div>CMDT</div>
  </div>
</body>
</html>



<!doctype html>
<html lang="fr">
<head>
  <meta charset="utf-8">
  <title>Facture {{num_cmdt}}{{#if id}} ({{id}}){{/if}}</title>
  <style>
    body { font-family: Arial, Helvetica, sans-serif; color: #111; margin: 24px; }
    .header { display: flex; align-items: center; margin-bottom: 16px; }
    .brand { color: #1a472a; font-weight: bold; font-size: 18px; }
    .muted { color: #666; font-size: 12px; }
    h1 { color: #1a472a; font-size: 20px; margin: 12px 0; }
    .kv { display: grid; grid-template-columns: 200px 1fr; gap: 8px 16px; margin: 12px 0 24px; }
    .kv .k { color: #1a472a; font-weight: bold; }
    table { border-collapse: collapse; width: 100%; font-size: 12px; }
    thead th { background: #1a472a; color: #fff; text-align: left; padding: 8px; }
    tbody td { padding: 8px; border-bottom: 1px solid #eee; }
    tr:nth-child(even) td { background: #f8f8f8; }
    .footer { display: flex; justify-content: space-between; margin-top: 16px; color: #666; font-size: 11px; }
  </style>
</head>
<body>
  <div class="header">
    <div>
      <div class="brand">COMPAGNIE MALIENNE POUR LE DÉVELOPPEMENT DU TEXTILE (CMDT)</div>
      <div class="muted">Export Facture</div>
    </div>
  </div>

  <h1>Détails de la facture</h1>
  <div class="kv">
    <div class="k">Référence</div><div>{{id}}</div>
    <div class="k">Numéro CMDT</div><div>{{num_cmdt}}</div>
    <div class="k">Numéro de facture</div><div>{{num_invoice}}</div>
    <div class="k">Fournisseur</div><div>{{supplier_name}}</div>
    <div class="k">Date d'arrivée</div><div>{{invoice_arr_date}}</div>
    <div class="k">Montant</div><div>{{amount}}</div>
    <div class="k">Statut DFC</div><div>{{dfc_status}}</div>
  </div>

  {{#if _attachments}}
  <h1>Pièces jointes</h1>
  <table>
    <thead>
      <tr><th>Nom</th><th>Type</th><th>Taille</th></tr>
    </thead>
    <tbody>
    {{#each _attachments}}
      <tr>
        <td>{{this.name}}</td>
        <td>{{this.type}}</td>
        <td>{{this.size}}</td>
      </tr>
    {{/each}}
    </tbody>
  </table>
  {{/if}}

  <div class="footer">
    <div>Généré le {{_generated_at}}</div>
    <div>CMDT</div>
  </div>
</body>
</html>



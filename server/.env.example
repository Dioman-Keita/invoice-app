# db config
DB_HOST=localhost
DB_PASSWORD=your_password
DB_NAME=cmdt_invoice_db
DB_USER=root
DB_PORT=3306

# app dependencies
APP_URL="invoice-app://"
JWT_SECRET_KEY=change_me_to_something_very_secure
NODE_ENV=development
UNVERIFIED_RETENTION_HOURS=1
LOGS_RETENTION_DAYS=30

# backend port
PORT=3000

# Email configuration
GMAIL_USER=your_email@gmail.com
GMAIL_PASS=your_app_specific_password

# origin
CORS_ORIGIN=http://localhost:5173
FRONTEND_URL=http://localhost:5173
COOKIE_DOMAIN=localhost

# Carbone configuration (local npm package)
CARBONE_TIMEOUT=120000
EXPORT_MAX_RECORDS=5000
